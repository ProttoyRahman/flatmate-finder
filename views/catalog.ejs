<!DOCTYPE html>
<html>
<head><title>Vacancy Catalog</title></head>
<body>
  <h1>Available Rooms</h1>
  <ul>
    <% listings.forEach(listing => { %>
      <li>
        <a href="/vacancies/<%= listing._id %>"><%= listing.title %></a>
        <strong><%= listing.location %></strong> — BDT <%= listing.rent %>/month
        <p><%= listing.description %></p>
        <p>Posted by: <%= listing.postedBy ? listing.postedBy.name : 'Unknown' %></p>
      </li>
    <% }) %>
  </ul>
  <a href="/" class="btn btn-outline-secondary">Back to Home</a>
  <% if (loggedIn) { %>
    <a href="/vacancies/post"><button>Post a Vacancy</button></a>
    <a href="/logout"><button>Logout</button></a>
  <% } else { %>
    <a href="/login"><button>Login to Post Vacancy</button></a>
  <% } %>

</body>
</html>
